extends layout

block content
  if repo
    p.debug RepoID: #{repo._id}
  if page
    p.debug PageID: #{page._id}
    div!= page.html

  form(method='POST')
    input(type='submit', value='edit this page')